
> 本文基于 **ECMA-262 最新标准规范**，从 **「认知/理解层面（开发者心智模型与语言设计全景）」** 与 **「底层/实现层面（规范算法与引擎运行时架构）」** 两个维度，深度解构 JavaScript/ECMAScript 的完整知识体系与执行机制。

---

## 目录

- [一、 维度一：认知/理解层面（开发者心智模型）](#一-维度一认知理解层面开发者心智模型)
  - [1. 认知维度知识全景脑图](#1-认知维度知识全景脑图)
  - [2. 模块一：类型与值语义系统](#2-模块一类型与值语义系统)
  - [3. 模块二：作用域、生命周期与模块化](#3-模块二作用域生命周期与模块化)
  - [4. 模块三：原型哲学与现代面向对象（OOP）](#4-模块三原型哲学与现代面向对象oop)
  - [5. 模块四：异步编程与控制抽象](#5-模块四异步编程与控制抽象)
  - [6. 模块五：标准内置库与元编程](#6-模块五标准内置库与元编程)
- [二、 维度二：底层/实现层面（规范算法与运行时架构）](#二-维度二底层实现层面规范算法与运行时架构)
  - [1. 实现维度架构全景脑图](#1-实现维度架构全景脑图)
  - [2. 模块一：形式文法与编译前端解析流水线](#2-模块一形式文法与编译前端解析流水线)
  - [3. 模块二：规范专用类型与抽象操作（AO）](#3-模块二规范专用类型与抽象操作ao)
  - [4. 模块三：执行上下文、环境记录与 Realm 模型](#4-模块三执行上下文环境记录与-realm-模型)
  - [5. 模块四：对象底层模型、基本内部方法与异质对象](#5-模块四对象底层模型基本内部方法与异质对象)
  - [6. 模块五：并发模型、共享内存与内存一致性](#6-模块五并发模型共享内存与内存一致性)
  - [7. 模块六：宿主分层机制与 Web 兼容扩展](#7-模块六宿主分层机制与-web-兼容扩展)
- [三、 双维联动：从代码编写到引擎执行的全生命周期映射](#三-双维联动从代码编写到引擎执行的全生命周期映射)
- [四、 基于双维联动的进阶学习体系与规划路线](#四-基于双维联动的进阶学习体系与规划路线)
  - [1. 学习方法论：双维交织递进法](#1-学习方法论双维交织递进法)
  - [2. 六阶循序渐进学习规划图](#2-六阶循序渐进学习规划图)
  - [3. 详细阶段规划与交付里程碑](#3-详细阶段规划与交付里程碑)

---

## 一、 维度一：认知/理解层面（开发者心智模型）

### 1. 认知维度知识全景脑图

> 💡 **Excalidraw 视觉白板架构图**：基于 `obsidian-excalidraw-plugin` 插件生成，存放于 `ECMAScript/Excalidraw/` 目录。在 Obsidian 中可直接预览，双击/点击即可进入白板自由拖拽编辑、调整排版与扩展双链。

![[Excalidraw/ECMAScript_认知维度架构.excalidraw|100%]]

---

### 2. 模块一：类型与值语义系统
- **原始类型（Primitive）vs 引用类型（Object）**：
  - 原始值（`Undefined`, `Null`, `Boolean`, `Number`, `BigInt`, `String`, `Symbol`）在逻辑上不可变（Immutable），按值传递；
  - 对象类型（`Object` 及子类型）按引用传递，拥有动态属性集合。
- **自动装箱（Auto-boxing）**：
  - 当在原始值上调用方法时（如 `'hello'.slice(1)`），引擎隐式将其封装为临时包装对象并在调用结束后丢弃。
- **隐式转换（Coercion）拓扑与协议**：
  - `ToPrimitive(input [, hint])`：优先调用 `[Symbol.toPrimitive](hint)`；若不存在，在 hint 为 `string` 时依次尝试 `toString()` $\rightarrow$ `valueOf()`；hint 为 `number`/`default` 时依次尝试 `valueOf()` $\rightarrow$ `toString()`。
- **四大判等模型对比**：

| 判等模型                | 规范操作名             | `NaN === NaN` | `+0 === -0` | 典型应用场景                           |
| :------------------ | :---------------- | :------------ | :---------- | :------------------------------- |
| ==                  | `IsLooselyEqual`  | `false`       | `true`      | 宽松类型转换判等                         |
| ===                 | `IsStrictlyEqual` | `false`       | `true`      | 常规业务严格判等                         |
| **`Object.is`**     | `SameValue`       | `true`        | `false`     | 精确区分正负零与 NaN                     |
| **`SameValueZero`** | `SameValueZero`   | `true`        | `true`      | `Map`/`Set` 键去重、`Array.includes` |

---

### 3. 模块二：作用域、生命周期与模块化
- **作用域进化与 TDZ**：
  - `var`：函数级作用域，声明前置提升（Hoisting）并初始化为 `undefined`，挂载至全局对象；
  - `let` / `const`：词法块级作用域（Block Scope），从块开始到声明语句执行前的区域为**暂时性死区（TDZ）**，提前访问触发 `ReferenceError`。
- **闭包（Closure）的心智模型**：
  - 函数在定义时捕获其外层词法作用域引用。即使外部函数调用栈已弹出，只要内部函数存在引用，外部环境记录（Environment Record）便在堆内存中长久存活。
- **ES 模块（ESM）的静态解析机制**：
  - **编译期静态图构建**：在代码执行前建立模块拓扑与导入/导出关系；
  - **符号活绑定（Live Binding）**：`import` 引入的是导出模块内部变量的实时动态引用，而非快照浅拷贝；
  - **现代增强**：支持动态 `import()` 条件加载与顶层 `await`（Top-level await）异步依赖扁平化。

---

### 4. 模块三：原型哲学与现代面向对象（OOP）
- **原型继承本质**：
  - JavaScript 中没有传统基于类（Class-based）的类型拷贝，而是通过对象间的隐式原型引用（`[[Prototype]]` / `__proto__`）构成的**委托原型链（Prototype Delegation Chain）**。
  - **属性屏蔽（Shadowing）**：向对象赋值属性时，若自身没有该属性且原型链上存在同名非只读属性，则在自身创建自有属性（Own Property），从而遮蔽原型链属性。
- **现代 Class 体系演进**：
  - **硬私有化（Private Members）**：`#field` 与 `#method` 基于引擎内部的 Private Names 机制，彻底阻断了 `Object.keys`、`getOwnPropertyNames` 或 `Proxy` 的外部反射；
  - **私有检查**：通过 `#x in obj` 语法安全判断对象是否持有特定私有字段；
  - **静态初始化块**：`static { ... }` 在类定义时评估执行一次，适合执行复杂的静态配置和私有权限共享；
  - **双向继承链**：`class Sub extends Super` 同时建立了子类构造函数原型链（`Sub.__proto__ === Super`）与实例原型链（`Sub.prototype.__proto__ === Super.prototype`）。

---

### 5. 模块四：异步编程与控制抽象
- **并发与事件循环心智模型**：
  - 主线程单线程执行，通过调用栈（Call Stack）、宏任务队列（Macrotask Queue）与微任务队列（Microtask Queue）协同驱动。
- **Promise 反应式状态机**：
  - 核心状态：`Pending` $\rightarrow$ `Fulfilled` / `Rejected`（单向且不可逆）；
  - **现代组合子矩阵**：
    - `Promise.all`：全部成功才成功，首个失败立即短路失败；
    - `Promise.allSettled`：等待全部兑现/拒绝，收集完整状态数组；
    - `Promise.race`：首个决议（无论成功失败）即完成；
    - `Promise.any`：首个成功即成功，全部失败返回 `AggregateError`；
    - `Promise.withResolvers`：直接解构获取 `{ promise, resolve, reject }`；
    - `Promise.try`：将同步/异步函数调用统一定制包装为 Promise 流程。
- **生成器与异步迭代流**：
  - `Generator`：通过 `yield` 实现协程级上下文挂起与双向数据交换；
  - `Async Generator` + `for-await-of`：实现背压可控的异步流式数据拉取。

---

### 6. 模块五：标准内置库与元编程
- **数组非破坏性更新（Immutable by Default）**：
  - `toSorted()`、`toReversed()`、`toSpliced()`、`with()` 均返回全新副本，不改变原数组。
- **弱引用集合与内存管理**：
  - `WeakMap` / `WeakSet`：键为弱引用（支持 Object 与非注册 Symbol），不阻碍垃圾回收；
  - `WeakRef` 与 `FinalizationRegistry`：支持观察对象 GC 行为并执行清理回调。
- **元编程与反射**：
  - `Proxy`：提供 13 种对象基本操作的底层陷阱（Trap）拦截；
  - `Reflect`：提供与 Proxy 陷阱一一对应的标准函数式反射接口。

---

## 二、 维度二：底层/实现层面（规范算法与运行时架构）

### 1. 实现维度架构全景脑图

> 💡 **Excalidraw 视觉白板架构图**：基于 `obsidian-excalidraw-plugin` 插件生成，存放于 `ECMAScript/Excalidraw/` 目录。在 Obsidian 中可直接预览，双击/点击即可进入白板自由拖拽编辑、调整排版与扩展双链。

![[Excalidraw/ECMAScript_引擎实现维度.excalidraw|100%]]

---

### 2. 模块一：形式文法与编译前端解析流水线（Clause 5, 8, 11~16）
- **上下文无关文法（CFG）与多重目标符号（Clause 5, 12）**：
  - 词法解析并非单一状态机。解析器根据当前语法上下文动态切换目标符号：
    - `InputElementDiv`：当前期待操作符时使用（将 `/` 解析为除法）；
    - `InputElementRegExp`：当前期待操作数时使用（将 `/` 解析为正则开头）；
    - `InputElementTemplateTail`：解析模板字符串后续片段；
    - `InputElementHashbangOrRegExp`：解析文件头 `#!` 注释。
- **自动分号插入（ASI）确定性规则（Clause 12.10）**：
  - **规则 1**：从左至右扫描，遇到不符合产生式语法的离格 Token 时，若其前存在换行符或为 `}`，则在当前位置虚拟插入分号；
  - **规则 2（受限产生式）**：语法中标记为 `[no LineTerminator here]` 的节点（如 `return`、`throw`、`break`、`continue`、`yield`、`arrowFunction =>`），若后紧跟换行符，必须立即插入分号。
- **句法导向操作（Syntax-Directed Operations）与早期错误（Clause 8）**：
  - 在代码真正运行前执行检查（Early Errors），如重复参数名、在非循环体中写 `break`、非法标签等；
  - 收集各类绑定名称表：`BoundNames`、`VarDeclaredNames`、`LexicallyDeclaredNames`。

---

### 3. 模块二：规范专用类型与核心抽象操作（Clause 6, 7）
- **规范类型（Specification Types，Clause 6.2）——仅存在于引擎概念中**：
  1. **Completion Record（完成记录，Clause 6.2.4）**：
     - 结构：`Completion { [[Type]], [[Value]], [[Target]] }`；
     - `[[Type]]` 为 `normal` 时代表顺序执行；为 `return`, `throw`, `break`, `continue` 时代表突变执行（Abrupt Completion），驱动控制流短路跳转。
  2. **Reference Record（引用记录，Clause 6.2.5）**：
     - 结构：`{ [[Base]], [[ReferencedName]], [[Strict]], [[ThisValue]] }`；
     - 延迟取值结构（区分左值 L-Value 与右值 R-Value）。在 `obj.fn()` 中，保留 `Base: obj`，供后续调用时将 `obj` 绑定为 `this`。
  3. **Property Descriptor（属性描述符，Clause 6.2.6）**：
     - 数据描述符（`[[Value]]`, `[[Writable]]`）与访问器描述符（`[[Get]]`, `[[Set]]`），附加元属性 `[[Enumerable]]`, `[[Configurable]]`。
  4. **Data Block（Clause 6.2.9）**：
     - 连续的底层字节内存块，为 `ArrayBuffer` 和 `SharedArrayBuffer` 提供物理承载。
- **核心抽象操作（Abstract Operations，Clause 7）算法化**：
  - `Get(O, P)`（Clause 7.3.2）：先查找自身再递归原型链，最终调用属性内部 getter 或返回值；
  - `Set(O, P, V, Throw)`（Clause 7.3.4）：依据原型链可写性及 setter 分支决定是创建自有属性还是报错；
  - `Call(F, V, [args])` / `Construct(F, [args], newTarget)`（Clause 7.3.13~14）：函数底层调用与实例化分流。

---

### 4. 模块三：执行上下文、环境记录与 Realm 模型（Clause 9, 16）
- **执行上下文（Execution Context，Clause 9.3）核心状态栈**：
  ```
  ┌──────────────────────────────────────────────────────────┐
  │                 Execution Context 结构                    │
  ├──────────────────────────────────────────────────────────┤
  │ • code evaluation state : 协程/生成器挂起与恢复的程序计数器  │
  │ • Function              : 当前执行的函数对象 (若有)       │
  │ • Realm                 : 当前执行所属的 Realm 实例        │
  │ • ScriptOrModule        : 当前执行所属的代码模块或脚本      │
  │ • LexicalEnvironment    : 词法环境 (查找 let / const / class) │
  │ • VariableEnvironment   : 变量环境 (查找 var / function)  │
  │ • PrivateEnvironment    : 私有名称环境 (查找 #privateField) │
  └──────────────────────────────────────────────────────────┘
  ```
- **环境记录（Environment Record，Clause 9.1）继承模型**：
  - `Declarative Environment Record`：纯内存符号表映射（性能极高）；
    - $\rightarrow$ `Function Environment Record`：维护 `[[ThisValue]]` 与 `[[ThisBindingStatus]]`（`"lexical"` 对应箭头函数，`"uninitialized"` 对应派生类构造函数）；
    - $\rightarrow$ `Module Environment Record`（Clause 16）：维护外部导出的不可变间接活绑定。
  - `Object Environment Record`：将属性读写委托给物理对象（用于 `with` 语句）；
  - `Global Environment Record`：**双核复合环境**，内含一个 `[[ObjectRecord]]`（映射全局 `globalThis` 对象）和一个 `[[DeclarativeRecord]]`（存放全局 `let`/`const`），实现全局顶级声明与全局对象属性的精准解耦。
- **Realm 与 Agent 模型（Clause 9.2, 9.4）**：
  - **Realm**：一个独立的执行沙箱，拥有独立的一整套固有对象池（Intrinsics，如独立的 `%Array.prototype%`）、全局对象与全局作用域；
  - **Agent**：一个执行线程单元，拥有独立的调用栈和微任务队列（`HostEnqueuePromiseJob`，Clause 9.5）。

---

### 5. 模块四：对象底层模型、基本内部方法与异质对象（Clause 10, 20, 23, 24）
- **11 个基本内部方法（Essential Internal Methods，Clause 10.1）**：
  - `[[GetPrototypeOf]]`, `[[SetPrototypeOf]]`, `[[IsExtensible]]`, `[[PreventExtensions]]`, `[[GetOwnProperty]]`, `[[DefineOwnProperty]]`, `[[HasProperty]]`, `[[Get]]`, `[[Set]]`, `[[Delete]]`, `[[OwnPropertyKeys]]`。
  - 函数对象额外拥有：`[[Call]]`（普通调用）、`[[Construct]]`（new 实例化）（Clause 10.2）。
- **对象不变性约束（Invariants of Essential Internal Methods，Clause 10.1）**：
  - 规范严格规定：任何扩展或代理逻辑，**绝不能破坏基本内部方法的不变性**。
  - 例如：如果 `[[GetOwnProperty]]` 报告某属性为不可配置且不可写，则后续所有 `[[GetOwnProperty]]` 必须始终报告该属性存在且拥有相同的值；否则引擎强制抛出 `TypeError`。
- **普通对象（Ordinary Object）vs 异质对象（Exotic Object，Clause 10.4, 10.5）**：

| 异质对象类型 | 拦截覆盖的基本内部方法 | 核心定制底层机制 |
| :--- | :--- | :--- |
| **Array Exotic** | `[[DefineOwnProperty]]` | 写入索引属性时动态同步调整 `length`；减少 `length` 时自动级联调用 `[[Delete]]` 销毁超出元素 |
| **String Exotic** | `[[GetOwnProperty]]` | 虚拟化字符数字索引，按需生成只读不可配置的字符数据描述符 |
| **Arguments Exotic** | `[[GetOwnProperty]]`, `[[DefineOwnProperty]]`, `[[Get]]`, `[[Set]]`, `[[Delete]]` | 非严格模式下通过参数映射对象（Parameter Map）保持形参与 `arguments` 双向绑定 |
| **Integer-Indexed (TypedArray)** | 全部 11 个内部方法 | 绕过哈希属性表，直接通过字节偏移量对底层的 `Data Block` 原生内存进行读写 |
| **Proxy Exotic** | 全部 11+2 个内部方法 | 将操作转发至用户定义的 Handler 陷阱函数，并在返回后强制执行规范不变性安全检查 |

---

### 6. 模块五：并发模型、共享内存与内存一致性（Clause 9.4~9.5, 25, 27, 29）
- **SharedArrayBuffer 与共享 Data Block（Clause 25.2）**：
  - 允许多个 Agent（如主线程与 Worker 线程）同时映射并访问同一块底层物理内存。
- **Atomics 与内存顺序（Clause 25.4 & Clause 29 Memory Model）**：
  - **Happens-Before 与 Synchronizes-With**：规范定义了形式化的读写事件偏序关系；
  - **防止乱序**：通过原子读（`Atomics.load`）、原子写（`Atomics.store`）和 CAS（`Atomics.compareExchange`），阻止多核 CPU 的乱序执行与流水线重排；
  - **线程同步**：`Atomics.wait` / `Atomics.notify` 提供底层 Futex 式的线程挂起与唤醒机制。

---

### 7. 模块六：元编程与宿主分层机制扩展（Clause 20.4, 26, 28, Annex B & D）
- **知名符号与弱引用内存管理（Clause 20.4, 26）**：
  - 知名符号（Well-Known Symbols）重定义内建对象行为；
  - `WeakRef` 与 `FinalizationRegistry` 提供观察垃圾回收机制的契约接口。
- **Annex D 宿主分层接入点（Host Layering Points）**：
  - 规范将特定环境相关操作抽象为 **Host Hooks**，由外部规范（如 WHATWG HTML 规范）填充具体实现：
    - `HostEnqueuePromiseJob`：微任务入队挂载；
    - `HostImportModuleDynamically`：动态 import 模块获取与编译接入；
    - `HostEnsureCanCompileStrings`：CSP 安全策略对 `eval` 和 `new Function` 的拦截检查。
- **Annex B 面向 Web 浏览器的附加规范（Legacy & Normative Optional）**：
  - 规范化历史存量网页所依赖的非标准特性（如 `Object.prototype.__proto__`、HTML 风格注释 `<!-- -->`、`RegExp.$1` 静态属性等），确保跨浏览器行为一致且不污染核心语言标准。

---

## 三、 双维联动：从代码编写到引擎执行的全生命周期映射

当我们在编辑器中写下一行简单的 JavaScript 代码时，两个维度在引擎内部以极其严密的机械齿轮结构紧密协同。

### 1. 全生命周期流程白板架构图

> 💡 **Excalidraw 视觉白板图**：已存放于 `ECMAScript/Excalidraw/` 目录。在 Obsidian 中可直接预览，双击/点击即可进入白板自由缩放、拖拽编辑与添加个人批注。

![[Excalidraw/ECMAScript_双维联动生命周期.excalidraw|100%]]

---

### 2. 逐阶段双维联动深度剖析（以 `user.name` 为例）

以如下最简代码为例：
```javascript
const user = { name: "Nate" };
user.name;
```

#### 阶段 1：词法与语法分析（Clause 11 ~ 12, Clause 8）
- **【认知层】**：开发者输入源代码文本，声明不可变常量 `user` 并访问属性 `name`。
- **【实现层】**：
  - **Scanner** 启动，当前语法上下文驱动目标符号切换为 `InputElementDiv`（Clause 12.2）；
  - 扫描字符流并生成 Token 序列：`[const, user, =, {, name, :, "Nate", }, user, ., name]`；
  - 语法解析器基于产生式验证无早期语法错误（Early Errors，Clause 8 句法导向操作），构建 AST（`VariableDeclaration` 与 `MemberExpression`，Clause 13~14）。

#### 阶段 2：作用域分析与字节码编译（Clause 8, 13 ~ 14）
- **【认知层】**：开发者在思维中建立块级作用域约束，明确 `user` 在当前块生效。
- **【实现层】**：
  - 静态分析 AST，收集 `BoundNames`，将 `"user"` 记录在 `LexicallyDeclaredNames` 集合中（Clause 8.1 静态语义）；
  - 引擎编译管道（如 V8 Ignition）将 AST 编译为字节码序列：
    - `CreateEmptyObjectLiteral` $\rightarrow$ `SetNamedProperty`
    - `LdaNamedProperty r0, [1]`（加载属性字节码指令）。

#### 阶段 3：运行时执行上下文入栈（Clause 9）
- **【认知层】**：代码进入执行阶段，为变量分配生命周期。
- **【实现层】**：
  - 引擎将当前代码段的 `ExecutionContext` 推入执行上下文栈（Clause 9.3 Call Stack）；
  - 绑定当前执行所属的 `Realm` 沙箱（Clause 9.2）与模块实例；
  - 初始化其 `LexicalEnvironment`，实例化一个 `Declarative Environment Record`（Clause 9.1）；
  - 在环境记录中为 `"user"` 分配不可变槽位，此时进入变量初始化，暂时性死区（TDZ）解除，将堆中新建对象的物理指针存入槽位。

#### 阶段 4：表达式求值产生引用记录（Clause 13.3, 6.2.5）
- **【认知层】**：执行 `user.name` 属性访问。
- **【实现层】**：
  - 评估 `MemberExpression`（Clause 13.3.7），引擎**并不直接返回值**，而是生成一个规范专用内部类型：
    $$\text{Reference Record} = \{ \text{[[Base]]: } \text{user (Object)}, \text{ [[ReferencedName]]: } \text{"name"}, \text{ [[Strict]]: } \text{false} \}$$（Clause 6.2.5）
  - 该引用完整保留了对象的基准上下文（Base），这一步是保证后续如果调用方法时 `this` 能够精准绑定的关键机制。

#### 阶段 5：抽象操作解包引用（Clause 6.2.5.6 & 7.3）
- **【认知层】**：等待属性返回实际值。
- **【实现层】**：
  - 引擎调用抽象操作 `GetValue(V)`（Clause 6.2.5.6）；
  - 识别 `V` 为引用记录（Reference Record），提取其 `[[Base]]` 与 `[[ReferencedName]]`；
  - 路由分发至核心对象抽象操作：`Get(O, P)`（Clause 7.3.2，其中 $O = \text{user}$, $P = \text{"name"}$）。

#### 阶段 6：对象基本内部方法查找（Clause 10.1）
- **【认知层】**：在对象的原型链和自有属性中寻找 `"name"` 键。
- **【实现层】**：
  - 抽象操作 `Get(O, P)` 下推调用对象自身的基本内部方法：`user.[[Get]]("name", user)`（Clause 10.1.8）；
  - `user` 为普通对象（Ordinary Object），执行默认的 `OrdinaryGet` 算法；
  - 触发 `user.[[GetOwnProperty]]("name")`（Clause 10.1.1），命中自身属性表中对应的属性描述符：
    $$\text{PropertyDescriptor} = \{ \text{[[Value]]: } \text{"Nate"}, \text{ [[Writable]]: } \text{true}, \text{ [[Enumerable]]: } \text{true}, \text{ [[Configurable]]: } \text{true} \}$$
  - （注：若自身不存在，则通过 `user.[[GetPrototypeOf]]()` 沿原型链自底向上递归查找）。

#### 阶段 7：完成记录打包与结果返回（Clause 6.2.4）
- **【认知层】**：表达式求值完毕，获得字符串 `"Nate"`。
- **【实现层】**：
  - 引擎将计算产物封装为标准的完成记录：
    $$\text{Completion Record} = \{ \text{[[Type]]: } \text{normal}, \text{ [[Value]]: } \text{"Nate"}, \text{ [[Target]]: } \text{empty} \}$$（Clause 6.2.4）
  - 执行上下文栈顶接收到 `normal` 类型的完成记录，控制流继续向前推进，将 `"Nate"` 投递给后续运算或控制台打印。

---

## 四、 基于双维联动的进阶学习体系与规划路线

### 1. 学习方法论：双维交织递进法

传统学习前端容易陷入两个极端：
- **纯 API 记忆（浅层浮躁）**：只背诵语法糖与怪异行为，遇到复杂性能瓶颈（如 Hidden Class 蜕化、跨帧闭包内存泄漏）时束手无策；
- **纯引擎源码（枯燥脱节）**：一头扎进 V8 几百万行 C++ 代码中，耗费海量时间却难以映射到日常架构设计、TypeScript 体系与前端复杂工程中。

**「双维交织递进法」**以 **ECMA-262 规范为唯一权威桥梁**，向上提炼**开发者心智模型**，向下推演**引擎物理实现**，形成不可撼动的底层认知闭环：

> 💡 **Excalidraw 视觉白板图**：已存放于 `ECMAScript/Excalidraw/` 目录。在 Obsidian 中可直接预览，双击/点击即可进入白板自由缩放与拖拽编辑。

![[Excalidraw/ECMAScript_双维交织方法论.excalidraw|100%]]

---

### 2. 六阶循序渐进学习规划图

> 💡 **Excalidraw 路线图白板**：已存放于 `ECMAScript/Excalidraw/` 目录。在 Obsidian 中可直接预览时间轴卡片流，双击/点击即可进入白板自由缩放编辑与勾选学习进度。

![[Excalidraw/ECMAScript_六阶学习路线规划.excalidraw|100%]]

#### 规划要素对照表（严格对照 ECMA-262 最新标准条款）

| 阶段      | 核心模块主题        | 认知层核心命题                      | 规范底层与引擎实现（最新条款对齐）                                                                                                | 预期落地文档产出             |
| :------ | :------------ | :--------------------------- | :--------------------------------------------------------------------------------------------------------------- | :------------------- |
| **第一阶** | **文法解析与编译前端** | 块级作用域、TDZ、声明提升与严格模式沙箱        | Clause 5 (记号)、Clause 8 (句法导向操作/Early Errors/BoundNames)、Clause 11~16 (源码/词法ASI/声明文法)                             | `03_形式文法与解析流水线.md`   |
| **第二阶** | **类型系统与抽象操作** | 7大原始类型、不可变性、装箱机制与4大判等模型      | Clause 6 (语言类型/Reference/Completion Record/Data Block)、Clause 7 (ToPrimitive/ToNumber/SameValue)                 | `04_规范类型与核心抽象操作.md`  |
| **第三阶** | **执行上下文与作用域** | 调用栈生命周期、闭包常驻机制、ESM 活绑定       | Clause 9 (执行上下文栈/环境记录树/双核全局环境/Realm沙箱)、Clause 16 (Scripts & Modules 活绑定)                                         | `05_执行上下文与作用域闭包.md`  |
| **第四阶** | **对象模型与异质对象** | 原型链查找/屏蔽、Class 私有字段、Proxy 拦截 | Clause 10 (普通对象与异质对象行为/11+2内部方法/Invariants不变性)、Clause 20 (基础对象)、Clause 23/24 (集合)                                | `06_对象底层模型与异质对象.md`  |
| **第五阶** | **异步控制与内存并发** | 事件循环微任务、Promise 状态机、协程与线程    | Clause 9.4~9.5 (Agent调度/JobQueue)、Clause 25 (SAB/Atomics)、Clause 27 (Promise/Generator)、Clause 29 (Memory Model) | `07_异步控制流与内存并发模型.md` |
| **第六阶** | **元编程与宿主环境**  | Reflect 反射、知名符号扩展、浏览器兼容边界    | Clause 20.4 (知名符号)、Clause 26 (WeakRef内存管理)、Clause 28 (Reflect/Proxy)、Annex D (Host Hooks)、Annex B (Web 历史遗留)     | `08_元编程与宿主分层扩展.md`   |

---

### 3. 详细阶段规划与交付里程碑

#### 第一阶：形式文法、词法扫描与编译前端（预计课时：1~2 周）
- **对应 ECMA-262 规范条款**：
  - **Clause 5**（Notational Conventions，上下文无关文法 CFG 与记号体系）；
  - **Clause 8**（Syntax-Directed Operations，静态语义 Static Semantics、Early Errors 早期错误拦截与 `BoundNames` / `LexicallyDeclaredNames` 收集）；
  - **Clause 11**（Source Text，Unicode 源码输入）；
  - **Clause 12**（Lexical Grammar，Scanner 模式动态切换 `InputElementDiv` / `InputElementRegExp`、ASI 自动分号插入算法与 `[no LineTerminator here]` 受限产生式）；
  - **Clause 13 ~ 16**（Expressions、Statements and Declarations、Functions and Classes、Scripts and Modules 句法体系）。
- **深度实践靶场**：
  - 使用 AST Explorer（Babel / Acorn）分析复杂语法的 AST 树结构；
  - 运行 `node --print-bytecode` 观察 V8 编译阶段如何对变量进行静态绑定。
- **阶段里程碑**：产出并归档文档 `03_形式文法与解析流水线.md`，配齐配套 Excalidraw 编译流水线图。

#### 第二阶：规范类型系统、内存模型与抽象操作（预计课时：1~2 周）
- **对应 ECMA-262 规范条款**：
  - **Clause 6.1**（Language Types，Undefined, Null, Boolean, String, Symbol, Number, BigInt, Object）；
  - **Clause 6.2**（Specification Types，Reference Record, Completion Record, Property Descriptor, Data Block）；
  - **Clause 7.1**（Type Conversion，`ToPrimitive`, `ToBoolean`, `ToNumeric`, `ToNumber`, `ToString`, `ToObject`）；
  - **Clause 7.2**（Testing and Comparison Operations，`SameValue`, `SameValueZero`, `IsStrictlyEqual`, `IsLooselyEqual`）；
  - **Clause 7.3 ~ 7.4**（Operations on Objects & Operations on Iterator Objects）。
- **深度实践靶场**：
  - 严格依据 ECMA-262 算法纯手写实现无缺陷的 `ToPrimitive` 与 `LooseEqual` 转换器；
  - 全量跑通前端经典黑魔法用例（如 `[] == ![]`、`{} + []`、`Object.is(-0, +0)`）。
- **阶段里程碑**：产出文档 `04_规范类型与核心抽象操作.md`。

#### 第三阶：执行上下文栈、环境记录与作用域闭包（预计课时：2 周）
- **对应 ECMA-262 规范条款**：
  - **Clause 9.1**（Environment Records，Declarative, Function, Module, Object 与 Global Environment Record 双核模型）；
  - **Clause 9.2**（Realms，固有对象池 Intrinsics 与独立全局作用域沙箱）；
  - **Clause 9.3**（Execution Contexts，执行上下文栈 Call Stack、`code evaluation state` 挂起恢复、`LexicalEnvironment`、`VariableEnvironment`、`PrivateEnvironment`）；
  - **Clause 9.4**（Jobs and Host Operations to Enqueue Jobs）；
  - **Clause 16**（Scripts and Modules，编译期静态依赖图、活绑定 Live Binding 物理实现）。
- **深度实践靶场**：
  - 使用 Chrome DevTools Memory 面板录制 Heap Snapshot，定位闭包引起的 Detached DOM 和堆内存泄漏；
  - 在 Node.js `vm` 模块中创建独立 `Context`，模拟规范级 Realm 之间的对象污染隔离。
- **阶段里程碑**：产出文档 `05_执行上下文与作用域闭包.md`。

#### 第四阶：对象内部模型、异质对象与原型体系（预计课时：2 周）
- **对应 ECMA-262 规范条款**：
  - **Clause 10.1**（Ordinary Object Internal Methods and Internal Slots，11 个核心对象基本内部方法与 `OrdinaryGet` / `OrdinarySet` 算法）；
  - **Clause 10.1**（Invariants of Essential Internal Methods，基本内部方法不变性安全底线原则）；
  - **Clause 10.2 ~ 10.3**（Function Objects，`[[Call]]` 与 `[[Construct]]` 内建函数逻辑）；
  - **Clause 10.4**（Built-in Exotic Object Behaviours，Array Exotic 动态 length 伸缩、String Exotic 虚拟字符索引、Arguments Exotic 参数双向绑定、Integer-Indexed Exotic 内存直通）；
  - **Clause 10.5**（Proxy Object Internal Methods and Internal Slots，13 个 Trap 陷阱分发与 Invariants 不变性强制核验）；
  - **Clause 20**（Fundamental Objects：`Object`, `Function`）、**Clause 23/24**（Indexed & Keyed Collections）。
- **深度实践靶场**：
  - 编写触发 V8 Shape（Hidden Class）变迁与 Dictionary Mode（字典模式退化）的性能对比基准测试；
  - 实现一个符合 ECMA-262 规范不变性检查的迷你 Proxy 运行时沙盒。
- **阶段里程碑**：产出文档 `06_对象底层模型与异质对象.md`。

#### 第五阶：异步控制流、事件循环与并发内存一致性（预计课时：2 周）
- **对应 ECMA-262 规范条款**：
  - **Clause 9.4 ~ 9.5**（Agents 线程单元与 `HostEnqueuePromiseJob` 微任务宿主注入）；
  - **Clause 25.2 & 25.4**（SharedArrayBuffer Objects 与 The Atomics Object 原子操作）；
  - **Clause 27.1**（Iteration Interfaces，同步与异步迭代协议）；
  - **Clause 27.2**（Promise Objects，PromiseReaction 反应记录、微任务排队与状态流转）；
  - **Clause 27.3 ~ 27.6**（GeneratorFunction 与 AsyncFunction 执行器机理）；
  - **Clause 29**（Memory Model，内存一致性模型、Happens-Before 偏序关系、Synchronizes-With 与数据竞争 Data Races 无冲突保证）。
- **深度实践靶场**：
  - 手写一个严格对齐 ECMAScript 规范行为（包括 `Promise.withResolvers` 与 `Promise.try`）的 Promise 实现；
  - 使用 Node.js Worker 线程 + `SharedArrayBuffer` + `Atomics.wait/notify` 编写无锁线程安全计数器。
- **阶段里程碑**：产出文档 `07_异步控制流与内存并发模型.md`。

#### 第六阶：元编程深度、标准内置库与宿主分层扩展（预计课时：1~2 周）
- **对应 ECMA-262 规范条款**：
  - **Clause 20.4**（Symbol Objects 与 Well-Known Symbols 知名符号协议）；
  - **Clause 26**（Managing Memory，`WeakRef` 与 `FinalizationRegistry` 弱引用及垃圾回收探针）；
  - **Clause 28**（Reflection，`Reflect` 13 个静态方法与 `Proxy` 的 1:1 对齐映射）；
  - **Annex D**（Host Layering Points，宿主分层钩子 `HostEnqueuePromiseJob`, `HostImportModuleDynamically`, `HostEnsureCanCompileStrings` 等）；
  - **Annex B**（Additional ECMAScript Features for Web Browsers，`Object.prototype.__proto__`、HTML 风格注释等 Web 遗留兼容规范）。
- **深度实践靶场**：
  - 使用 `Proxy` + `Reflect` + `WeakMap` 构建一个高精度、深层无内存泄露的响应式追踪引擎（对标 Vue 3 Reactivity 内核）；
  - 利用 `FinalizationRegistry` 编写大对象生命周期探针与自动资源释放管理器。
- **阶段里程碑**：产出文档 `08_元编程与宿主分层扩展.md`，达成 ECMAScript 全景体系贯通！
